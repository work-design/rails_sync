<div data-controller="menu empty" class="menu ex-accordion is-active">
  <div class="menu-label">
    <div>
      <i class="fa-solid fa-fw fa-sitemap"></i>
      <span><%= t('sync.admin.title') %></span>
    </div>
    <%= link_to({ controller: 'sync/admin/home' }, aria: { label: '设置' }) do %>
      <i class="fa-solid fa-cog"></i>
    <% end %>
  </div>
  <div class="menu-list" data-empty-target="content">
    <%= link_to t('sync.admin.apps.index.title'), { controller: 'sync/admin/apps' }, data: { turbo_frame: 'body', action: 'menu#enter' }, class: active_helper(controllers: ['apps'], active: 'is-active') %>
  </div>
</div>
